import { ARTIR, AZALT, RESET } from "../types/types"

export const artir = () => {
    return{
        type:ARTIR
    }
}
export const azalt = () => {
    return{
        type:AZALT
    }
}
export const reset = () => {
    return{
        type:RESET
    }
}